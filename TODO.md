<!-- https://github.com/GatlenCulp/gatlens_prompt_engineering -->
// TODO: [typst-template] Perhaps make the above assignments permanent?

// TODO: [typst-template] Look into elembic for writing libraries https://pgbiel.github.io/elembic/elements/creating/declaring.html

TODO: Check out theseee https://search.nixos.org/packages?channel=unstable&query=typstPackages
// TODO: [typst-template] Figure out how to automatically configure rainbow brackets + progressively larger brackets when nested regardless of the size.

// TODO: Learn how to prevent typst preview from clicking through and going to some definition in the package.

## AI Tags

// TODO: [typst-template] Maybe add a set of AI tags. E.g. (AI: ignore on a line you don't want commentary on)
// TODO: [typst-template] Tell AI to leave specific TODO comments in a format where things must be returned to

## Language Extensions

### Semantic Type Checking
// TODO: [typst-template] Maybe look into SemanTeX for inspiration https://ctan.math.washington.edu/tex-archive/macros/latex/contrib/semantex/semantex.pdf
// TODO: [typst-template] Perhaps have some way to say, within a section, that theta be overwritten to be CONST(theta) or RV(theta) with a specific type of styling for consistency. Added benefit that you can remove this and it still works fine. Would probably need to be performant. Assume the type of vars from the context or notation (e.g. vb means vector). Maybe also allows you to add alternative names/descriptions as well. A nice way of printing it too.
// TODO: [typst-template] Maybe also have some parsing for syntactical errors as well in combination with the above? Kind of like how python has many linters that will identify static errors (like obviously if you define alpha to be a constant and you say a in alpha then that's obviously quite sus. But will still render correctly. Like you can type your function wrong but it still runs correctly in runtime.)
// TODO: [typst-template] Maybe for my typing, use a different math font for verified/auto-styled variables?

### Misc
// TODO: [typst-template] Develop an "expand this variable" operator? Or maybe in vscode something that does this manually

<!-- // TODO: [typst-template] Is there a way of making it such that Pr() = Pr[] = Pr{} and can call functions with each? Answer: No -->


// TODO: [typst-template] Fix ++, **, etc. such that I can actually force it to display the right symbols.


## General

// TODO: [typst-template] Improve performance of my template.
// TODO: [typst] Understand how to change the equation numbering to actually give the equation the name of the label or something.

## Styling
// TODO: [typst-template] Make equation numbering smaller + grey
// TODO: [typst-template] Define a serif, sans-serif, mono, etc. font. Then can use as I please.
// TODO: [typst-template] Are there cooler math fonts than the default? What would the purpose do each serve?
// TODO: [typst-template] Increase text size or inset for the question to it is easier to read.

## Homework
// TODO: [typst-template] Make level two problems take up one column instead of two.

<!-- // TODO: [typst-template] Learn how to just get a warning on a given line (not panic) Answer: Note possible. Will have in the future. For now just panic. -->

// TODO: [typst-template] Fix reference syntax.

## Typst-Tooling
<!-- // TODO: [typst-tooling] Check if you can use lean inside of typst. OR if you could use some kind of lean notebook or something and display it in Typst. ANSWER: No. No lean Jupyter Notebook kernel or anything like that -->

<!-- // TODO: [typst-tooling] Check if there's a way to get typstyle to improve the formatting of math code blocks automatically Answer: No. Very minimal configuration https://typstyle-rs.github.io/typstyle/installation.html -->


